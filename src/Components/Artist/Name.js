import styled from "styled-components";

const Name = styled.h1`
  color: ${props => props.theme.primary};
  font-size: 40px;
  font-weight: bold;
  width: 100%;
  margin: auto;
  margin-top: 15px;
`;

export default Name;
